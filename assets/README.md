# Assets

Reserved for future static assets (images, downloadable diagrams).

The site currently needs nothing from this folder: every icon is inline SVG and the
favicon is an inline data URI, so `index.html`, `style.css` and `script.js` are enough
to run the whole website.
